before_install:
  - sudo apt-get -y install libxml2-dev
language: node_js
node_js:
    - "13.3.0"
script:
    - "npm install -g pxt"
    - "pxt target microbit"
    - "pxt install"
    - "pxt build"
sudo: false
cache:
    directories:
    - npm_modules
    - pxt_modules
